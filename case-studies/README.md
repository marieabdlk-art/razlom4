# Case studies

These traces preserve the protocol's reasoning process, including rejected
candidates and negative results. They are evidence of process behavior, not a
claim that RAZLOM-4 outperforms its controls.

- `origin/` — the debate from which the method was extracted.
- `partial-failure/` — blind recovery pilot ending in `NO_VALID_MUTATION`.
